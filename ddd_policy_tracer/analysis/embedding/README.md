A module that generates a text embedding for a piece of text. The embedding is a vector representation of the text that captures its meaning and can be used for various natural language processing tasks, such as clustering, classification, and similarity analysis.

There should be three types of embedding:

1. document embedding
2. chunk embedding
3. claim embedding